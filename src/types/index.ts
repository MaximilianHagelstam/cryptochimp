export type OwnedCoin = {
  name: string;
  symbol: string;
  quantity: number;
  currentPrice: number;
  percentChange1h: number;
  percentChange24h: number;
  percentChange7d: number;
  totalValue: number;
};
