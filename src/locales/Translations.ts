export default interface Translations {
  common: {
    logout: string;
  };
  error: {
    notFound: {
      title: string;
      description: string;
      goHome: string;
    };
  };
  navLinks: {
    dashboard: string;
    wallet: string;
    transactions: string;
    market: string;
    trade: string;
  };
}
